'use strict'

import fs from 'fs'

export function serialize(filename, json){
	return new Promise((resolve, reject) => {
		fs.writeFile(filename, JSON.stringify(json), (err) => {
			if (err) reject(`Error while generating file ${filename}`, err)

			resolve(`File ${filename} saved!`)
		})
	})
}